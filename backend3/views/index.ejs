<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main text-xl w-full min-h-screen bg-zinc-900 flex items-center justify-center align-baseline flex-col">
        <div class=" form w-1/2 bg-zinc-600 p-4 rounded-lg">
            <form action="/create" method="post" class=" p-2 flex flex-col gap-4">
                <input type="text" placeholder="Title goes here" class="text-white input bg-zinc-800 rounded p-2 " name="title" />
                <textarea class="rounded p-2 bg-zinc-800 text-white" placeholder="write your task here" name="details" id=""></textarea>
                <input type="submit" value="Create Task" class="btn border-zinc-800 rounded p-2 bg-zinc-900 text-zinc-200 hover:bg-zinc-700 trans transition duration-200 cursor-pointer" />
            </form>
        </div>

        <div class="tasks  p-2 rounded-lg m-2 flex flex-wrap gap-">

            <% if(files.length>0){ %>
                <% files.forEach(function(val){ %>
                    <div class="task p-4 w-90 bg-zinc-600 bg-zinc-600 p-2 rounded-lg m-2">
                        <h2 class="text-xl text-zinc-100"><%= val %></h2>
                        <p class="text-sm">Task Description</p>
                        <div class="flex w-full justify-between">
                            <a class="text-blue-600" href="/file/<%= val %>">Read More...</a>
                            <a href="/edit/<%=val%>" class="px-2 text-zinc-800">Edit File Name</a>
                        </div>
                    </div>
        

               <% })  %>
            <% } else{ %>
                <h3 class="text-zinc-200" >No task more yet.</h3>
            <% } %>

        </div>
    </div>


    <script src="/javascripts/script.js"></script>
</body>
</html>