<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class=" w-full min-h-screen p-5 text-xl text-zinc-100 main bg-zinc-900 flex flex-col">
        <a href="/" class="text-2xl text-blue-500 mb-3">Go to Home</a>
        <h1 class="text-2xl">All Users</h1>
        <div class="users flex flex-wrap gap-3">
            <% users.forEach(function(user){ %>
                <div class="user w-62 p-4 bg-zinc-800 rounded-lg m-2">
                    <div class="w-full h-52 rounded-lg bg-zinc-500">
                        <img class="w-full h-full object-cover rounded " src="<%= user.image%>" alt="">
                    </div>
                    <h3 class="text-xl tracking-tighter mt-3"><%= user.name%></h3>
                    <h4 class="email text-zinc-600"><%= user.email%></h4>
                    <div class="buttom flex justify-between gap-3">
                        <a href="/edit/<%=user._id%>" class="text-zinc-300">Edit User</a>
                        <a href="/delete/<%= user._id%>" class="text-red-500">Delete User</a>
                    </div>
                </div>
            <% })%>
        </div>

    </div>
</body>
</html>